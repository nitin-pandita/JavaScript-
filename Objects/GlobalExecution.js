hey();
// And I'm calling it here but still JS will call the function as it already knows about the function that we have create before actually creating it.


// This is called hosted

// My function is here 
function hey() {
    console.log('Hello world');
}